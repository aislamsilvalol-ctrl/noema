{
  "type": "object",
  "properties": {
    "belief": { "type": "string" },
    "questions": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "type": { "type": "string", "enum": ["mcq", "true_false"] },
          "prompt": { "type": "string" },
          "options": { "type": "array", "items": { "type": "string" } },
          "correct_index": { "type": "integer", "minimum": 0 },
          "answer": { "type": "boolean" },
          "explanation": { "type": "string" },
          "discriminates": { "type": "string" }
        },
        "required": ["type", "prompt", "explanation", "discriminates"]
      }
    }
  },
  "required": ["belief", "questions"]
}
